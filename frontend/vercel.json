{
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/static-build", "config": {} }
  ],
  "routes": [
    { "source": "/(.*)", "destination": "/frontend/$1" }
  ],
  "outputDirectory": "frontend"
}
